<%= form_for @artist  do |f| %>

  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@artist.errors.count, "error") %> encountered:
      </h2>
      <ol>
        <% @artist.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ol>
    </div>
  <% end %>

  <div class="form-group <%= 'field_with_errors' if @artist.errors[:name].any? %>">
    <%= f.label :name %>
        <br />
    <%= f.text_field :name %>
  </div>

    <%= f.submit class: "btn btn-primary" %>
<% end %>
